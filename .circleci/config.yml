version: 2.1
jobs:
  build-and-test:
    working_directory: ~/project
    docker:
      - image: circleci/python:3.7.6
    steps:
      - checkout
      - run:
          name: Runnning tests
          command: |
            pip install --upgrade pip
            pip install -r packages/requirements.txt
            export PYTHONPATH=./packages/titanic
            python packages/titanic/titanic/train_pipeline.py
            pytest -s packages/titanic/tests/
  build-docker-image:
    working_directory: ~/project
    docker:
      - image: circleci/python:3.7.6
    steps:
      - checkout
      - setup_remote_docker:
          docker_layer_caching: true
      - run:
          name: Building Docker Image
          command: |
            docker build -t mattague/titanic .
  push-docker-image:
    working_directory: ~/project
    docker:
      - image: circleci/python:3.7.6
    steps:
      - checkout
      - setup_remote_docker:
          docker_layer_caching: true
      - run:
          name: Pushing Docker Image
          command: |
            docker login --username=$DOCKERHUB_USERNAME --password=$DOCKERHUB_PASS
            docker push mattague/titanic


workflows:
  main:
    jobs:
      - build-and-test
      - build-docker-image
      - push-docker-image:
          requires:
            - build-and-test
            - build-docker-image
